import React from 'react';
//import classes from './ContentWrapperLeft.module.css';
import classes from '../AdventureWrapper.module.css';

const ContentWrapperLeft = (props) => (
    <div className={`${classes.ContentWrapper} ${classes.WrapperLeft}`}>hi</div>
);

export default ContentWrapperLeft;