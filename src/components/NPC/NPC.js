import React from "react";
import Card from "../Card/Card";

const NPC = (props) => {
  return (
    <Card>
      <h4>NPC name</h4>
      <p className="CardSubtitle">Card Subtitle</p>
    </Card>
  );
};

export default NPC;
